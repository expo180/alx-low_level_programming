#include "main.h"
/**
 * is_upper - This function checks if a character is uppercase or not
 *
 * Return: 1 if c is uppercase
 */
int _isupper(int c)
{
	int c;

	if (c >= 'A' && c <= 'Z')
		return 1;
	else
		return (0);
	_purchar('\n');
}
