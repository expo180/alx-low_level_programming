#include <stdio.h>

/**
 * main - Entry of the program
 *
 * Return: Success(0)
 */

int main(void)
{
	int m;
	float z;
	char u;

	printf("The adress memory of int m is %p\n", &m);
	printf("The adress memory of float is %p\n", &z);
	printf("The adress memory of char is %p\n", &u);
	return (0);
}
